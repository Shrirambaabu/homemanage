<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="srb.homemanagement">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/expense_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/expense_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ExpenseListActivity"
            android:label="Expense Lists" />
        <activity
            android:name=".activity.AddExpenseActivity"
            android:label="Add Expense" />
        <activity android:name=".activity.EditExpenseActivity" android:label="Edit Expense"/>
    </application>

</manifest>